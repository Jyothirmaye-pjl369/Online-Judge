const express = require('express');
const router = express.Router();

// All code execution logic is now handled in code-runner/submitRoute.js
// This file is intentionally left blank to avoid duplicate/incorrect logic.

module.exports = router;
